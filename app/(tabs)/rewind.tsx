import { PageLayout } from '@/src/components/ui/page-layout';
import { KurRewindTab } from '@/src/components/library/kur-rewind-tab';

export default function RewindScreen() {
	return (
		<PageLayout edges={[]}>
			<KurRewindTab />
		</PageLayout>
	);
}
